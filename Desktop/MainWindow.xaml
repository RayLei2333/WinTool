<Window x:Class="Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Desktop"
        xmlns:converts="clr-namespace:Desktop.Converters" 
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450" 
        Width="800"
        BorderThickness="0" 
        WindowStyle="None"   
        AllowsTransparency="True" 
        Background="{Binding BackgroundImage}"
        WindowStartupLocation="CenterScreen" 
        x:Name="mainWindow"
        >
    <!--WindowStyle="None"   AllowsTransparency="True"  Background="{Binding BackgroundImage}"-->
    <!--<Grid x:Name="grid">-->
    <!--<TextBlock x:Name="logs"></TextBlock>-->
    <!--</Grid>-->
    <!--<Button Click="Button_Click">aaa</Button>-->
    <Window.Resources>
        <converts:ValueEqualsConverter x:Key="ValueEqualsConverter"></converts:ValueEqualsConverter>
    </Window.Resources>

    <Grid>
        <WrapPanel x:Name="wrapper">

        </WrapPanel>


        <ListView ItemsSource="{Binding DesktopFile}" Padding="0" Margin="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
                
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!--<Border Width="{Binding DataContext.IconWidth, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.IconWidth, RelativeSource={RelativeSource AncestorType=Window}}">-->
                    <StackPanel Width="{Binding DataContext.IconWidth, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.IconWidth, RelativeSource={RelativeSource AncestorType=Window}}">
                        <!--<Image Source="{Binding Icon}" Width="40" Height="40"/>-->
                        <TextBlock Text="{Binding DataContext.IconWidth, RelativeSource={RelativeSource AncestorType=Window}}" TextAlignment="Center"/>
                    </StackPanel>
                    <!--</Border>-->
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ListView>

        <!--自动对齐网格-->
        <!--<ListView ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled" ItemsSource="{Binding DesktopFile}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="10"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate >
                    <Border Width="{Binding DataContext.IconWidth, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.IconWidth, RelativeSource={RelativeSource AncestorType=Window}}">
                        <TextBlock Text="{Binding Name}"></TextBlock>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
    </Grid>
</Window>
