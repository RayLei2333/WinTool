<UserControl x:Class="Desktop.Views.DesktopIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800"  
             x:Name="Root" 
             Width="{Binding Width}"
             Height="{Binding Height}"
             Margin="0"
             Padding="0"
             BorderThickness="0">

    <Grid ></Grid>
    <!--<StackPanel Orientation="Vertical" Margin="0">
        <Image x:Name="IconImage"
               Stretch="Uniform"
               Width="43"
               Height="43"
               Source="{Binding IconSource, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Bottom"/>

        <TextBlock x:Name="textShortName" 
                   Text="{Binding ShortName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{StaticResource desktopFileNameStyle}" 
                   Visibility="{Binding ShortNameVisibility,RelativeSource={RelativeSource AncestorType=UserControl}}">
            <TextBlock.Effect>
                <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Direction="1" Opacity="1" />
            </TextBlock.Effect>
        </TextBlock>

        <TextBlock x:Name="textFullName"
                   Text="{Binding FullName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{StaticResource desktopFileNameStyle}" 
                   Visibility="{Binding FullNameVisibility,RelativeSource={RelativeSource AncestorType=UserControl}}">
            <TextBlock.Effect>
                <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Direction="1" Opacity="1" />
            </TextBlock.Effect>
        </TextBlock>
    </StackPanel>-->

    <!--<TextBlock Text="{Binding ShortContent, RelativeSource={RelativeSource AncestorType=UserControl}}"
             Margin="0,3,0,0"
             TextAlignment="Center"></TextBlock>-->

    <!--<TextBlock Text="{Binding FullContent}"
             Margin="0"
             TextAlignment="Center"
             TextWrapping="Wrap"></TextBlock>-->
    <!--<local:DesktopIconName x:Name="desktopName"
                         TextContent="{Binding IconName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         TextAlignment="Center" 
                         Foreground="#fff"
                         Margin="0,3,0,0"
                         TextWrapping="Wrap">
      <local:DesktopIconName.Effect>
          <DropShadowEffect Color="Black" BlurRadius="0" ShadowDepth="1" Direction="0" Opacity="1" />
      </local:DesktopIconName.Effect>

  </local:DesktopIconName>-->
</UserControl>
