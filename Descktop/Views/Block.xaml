<UserControl x:Class="Desktop.Views.Block"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" 
             d:DesignWidth="400">
    <UserControl.Resources>
        <Style x:Key="TopBarButton" TargetType="Button">
            <Setter Property="Width" Value="30"></Setter>
            <Setter Property="Height" Value="30"></Setter>
            <Setter Property="Foreground" Value="#fff"></Setter>
            <Setter Property="Background" Value="Transparent"></Setter>
            <Setter Property="BorderThickness" Value="1"></Setter>
            <Setter Property="FontSize" Value="15"></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Button Style="{StaticResource TopBarButton}" HorizontalAlignment="Left" FontFamily="{StaticResource IconFont}" Content="&#xe600;"></Button>
            <TextBlock Text="123333" VerticalAlignment="Center" Foreground="#fff" HorizontalAlignment="Center" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <!--<Button Style="{StaticResource TopBarButton}" Content="Right 1" />-->
                <Button Style="{StaticResource TopBarButton}" FontFamily="{StaticResource IconFont}"  Content="&#xe601;" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
